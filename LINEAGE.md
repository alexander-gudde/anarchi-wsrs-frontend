# Lineage

Git history and the package lock are the authoritative rebuild lineage. The reconciliation preserved frontend behavior while replacing the retired Render backend origin with the secured same-origin `/wsrs` path.

Presentation remains separate from report generation, payments, transport, and orchestration. No legacy backend origin is an allowed runtime dependency.
